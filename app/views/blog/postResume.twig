<a href="/blog/article-{{ post.id }}">
    <h2>{{ post.title|title | truncate(15, true) }}</h2>
</a>
<h3>Published on {{ post.published_at|date('d/m/Y H:i') }}.</h3>

<p>
    {{ post.content|nl2br | truncate(30, true) }}
</p>
